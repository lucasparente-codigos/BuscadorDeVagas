# JobFinder

Buscador de vagas para iniciantes em TI (Help Desk, Suporte, Estágio, etc.)

## Como rodar

### Backend (Python)
```
cd backend
pip install -r requirements.txt
uvicorn main:app --reload
```

### CLI (Go)
```
cd cli
go run main.go search -t "help desk"
```
